{
  "name": "@coro-ai/desktop-electron",
  "private": true,
  "version": "0.1.0",
  "description": "Coro Desktop Shell — Electron wrapper for the Coro runner and dashboard",
  "license": "BUSL-1.1",
  "author": "Coro",
  "main": "dist/main.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm run build:bundle && pnpm run prepare:icon && pnpm run prepare:resources",
    "build:bundle": "tsup src/main.ts src/preload.ts --format cjs --out-dir dist --clean --target node20 --external electron --sourcemap",
    "prepare:icon": "node ./scripts/prepare-icon.mjs",
    "prepare:resources": "node ./scripts/prepare-resources.mjs",
    "dev": "pnpm run build && electron .",
    "typecheck": "tsc --noEmit",
    "package:mac": "pnpm run build && electron-builder --config electron-builder.json5 --mac dir --arm64 --publish never",
    "dist:mac": "pnpm run build && electron-builder --config electron-builder.json5 --arm64 --publish never",
    "release:mac": "pnpm run build && electron-builder --config electron-builder.json5 --arm64 --publish always",
    "package:win": "pnpm run build && electron-builder --config electron-builder.json5 --win dir --x64 --publish never",
    "dist:win": "pnpm run build && electron-builder --config electron-builder.json5 --win nsis --x64 --publish never",
    "release:win": "pnpm run build && electron-builder --config electron-builder.json5 --win nsis --x64 --publish always",
    "clean": "node -e \"const fs=require('node:fs'); fs.rmSync('dist',{recursive:true,force:true}); fs.rmSync('release',{recursive:true,force:true});\""
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "electron": "43.3.0",
    "electron-builder": "^26.15.3",
    "png-to-ico": "^3.0.2",
    "sharp": "^0.35.3",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2"
  }
}